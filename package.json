{
  "type": "module",
  "scripts": {
    "db:init": "node server/src/db/init.mjs",
    "db:seed": "node server/src/db/seed.mjs",
    "db:reset": "rimraf server/src/db/feedback.db server/src/db/feedback.db-wal server/src/db/feedback.db-shm && node server/src/db/init.mjs && node server/src/db/seed.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.0"
  }
}
